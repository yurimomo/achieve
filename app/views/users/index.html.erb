<div class="container">
 <h2>ユーザー</h2>

 <% @users.each do |user| %>
  <div class="container-child col-md-2 col-sm-4 col-xs-6">
  
   <%= profile_img(user) %>
   <%= user.email %><br>
   <%= link_to "詳細ページ", user_path(:id) %>
   <%= render 'follow_form' , user: user %>
  </div>
 
  <% end %>
</div>